# 协作方式

通过 Issue 明确问题、因素、依赖和验收；通过 PR 修改文档或实现；重要职责和合同变化通过 ADR 评审。

建议三条工作线：Agent/上下文，PyCircuit/编译器，验证/实验。架构负责人维护领域契约；验证负责人维护冻结任务与最终验收。这是责任建议，不是人员任命。

每个实验说明同模型、同任务、同预算下改变了哪个因素。专家 Prompt 与 Agent 自写解析脚本是允许的强基线。报告不成功样本和准备成本，不只展示成功候选。

发布前检查内部链接、JSON 语法、敏感字段、资料授权、目标答案泄漏，以及“已实现/已验证”表述是否有证据。不要把本地运行路径或会话附件链接当成公开证据地址。

任务索引见 [BACKLOG](tracking/BACKLOG.md)，决策见 [ADR](docs/adr/README.md)。

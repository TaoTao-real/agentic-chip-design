# ADR-006：Runtime 仅作薄适配

状态：Proposed。日期：2026-09-23。

建议：领域契约与状态独立于 DSH/CHIA 内部对象；更换 runtime 作为单独实验因素。

替代：直接耦合可少写短期胶水，但上游 API 变化会牵动领域层。

代价与验收：评估后重准备、模型前轻交付；固定上游版本，测试持久化、取消、并行、恢复和压缩。

关联：ACD-008。批准人和日期待补。

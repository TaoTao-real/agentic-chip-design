# 数据保护与知识隔离

## 公开范围

允许发布经整理的架构、通用接口草案、评估方法、阶段摘要与任务。当前公开内容不是完整私有初始化包的镜像。

不发布凭据、账号登录配置、完整会话、个人邮箱、机器/网络信息、许可证服务信息、受限 PDK、原始候选补丁、大型波形和完整 EDA 日志。需要公开新资料时单独审核；不因仓库已公开就默认所有后续附件可以公开。

## 盲测隔离

规划文档、目标专用 Episode 与历史补丁不挂载到盲测设计工作区。强基线和实验组只访问冻结的知识清单。记录实际读取/注入的知识版本、字段和哈希，而不只记录访问权限。

## 工程边界

Agent 不得修改 golden、合同、测试、评分器或历史证据。准备层只能读取封存产物；将日志内容当作不可信数据。校验候选、环境、单位与阶段，显式报告 missing 和 ambiguous。

本地 ignore 规则不是保密控制。上传前仍需内容审核。不要在公开 Issue 中粘贴敏感错误日志或访问凭据。
